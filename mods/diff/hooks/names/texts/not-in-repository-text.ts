/**
 * `/diff`'s answer outside a git repository, worded as the built-in words it.
 */
export const NOT_IN_REPOSITORY_TEXT =
  'The diff panel shows git changes — the current directory isn’t in a ' +
  'git repository'
