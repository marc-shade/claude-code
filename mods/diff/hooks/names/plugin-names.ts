/**
 * The names the engine may stamp on the elements this plugin's render hooks
 * draw (`ui.press`, `ui.focus`): its own, and the one a build that carries
 * it registers it under.
 */
export const PLUGIN_NAMES = ['diff', 'cc-plugin-diff'] as const
